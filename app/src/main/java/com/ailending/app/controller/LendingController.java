package com.ailending.app.controller;

/**
 * Placeholder — superseded by focused controllers:
 * <ul>
 *   <li>{@link IngestionController} — {@code POST /api/lending/ingest}</li>
 *   <li>{@link QueryController}     — {@code POST /api/lending/query}</li>
 *   <li>{@link LoanController}      — {@code /api/lending/loans/**}</li>
 * </ul>
 *
 * <p>Retained as an empty class to avoid breaking any external reference. It may be
 * removed in a future clean-up pass once it is confirmed that nothing references it.
 */
public class LendingController {
    // All endpoints have been moved to the focused controllers above.
}
